name: Maestro Tests

on:
  # pull_request:
  #   types: [labeled, synchronize]
  # issue_comment:
  #   types: [created]
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  maestro:
    uses: DleanJeans/expo-workflows/.github/workflows/maestro-tests.yml@main
