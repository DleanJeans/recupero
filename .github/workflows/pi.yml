name: Pi Agent

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  pi:
    uses: DleanJeans/expo-workflows/.github/workflows/pi.yml@main
    secrets:
      api-key: ${{ secrets.OPENCODE_API_KEY }}
